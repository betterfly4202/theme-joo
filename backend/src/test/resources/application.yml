# Test
spring:
  profiles:
    active: local # 기본 환경
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

---
spring:
  profiles: local
  jpa:
    show-sql: true